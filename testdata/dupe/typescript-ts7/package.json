{
  "name": "dupe-metric-testdata-typescript-ts7",
  "private": true,
  "description": "Fixture package with TypeScript 7 (no classic compiler API) plus the official @typescript/typescript6 compat shim, so internal/tokenizers/typescript's tests can verify scanner.js's fallback path actually works against a real TS7 install.",
  "dependencies": {
    "typescript": "^7.0.0",
    "@typescript/typescript6": "^6.0.0"
  }
}
