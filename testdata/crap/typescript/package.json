{
  "name": "crap-metric-testdata-typescript",
  "private": true,
  "description": "Fixture package so internal/analyzers/typescript's tests can resolve a real installed `typescript` (classic compiler API, 4.x/5.x) the same way a target repo would.",
  "dependencies": {
    "typescript": "^5.9.3"
  }
}
