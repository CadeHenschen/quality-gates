// Under a Tests/ directory (SPM convention) — must be skipped entirely,
// even though this filename itself doesn't match the *Tests.swift suffix.
func helperFunc() {
    if true {
        print("helper")
    }
}
